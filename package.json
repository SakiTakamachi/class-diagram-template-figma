{
  "name": "Class-Diagram",
  "version": "1.0.0",
  "description": "Your Figma widget",
  "scripts": {
    "build": "esbuild widget-src/code.tsx --bundle --outfile=dist/code.js  --minify --target=es6",
    "tsc": "tsc --noEmit -p widget-src",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.77.0",
    "@figma/widget-typings": "^1.8.0",
    "esbuild": "*",
    "typescript": "*"
  }
}
